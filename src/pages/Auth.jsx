import { Login } from '@components/auth/Login';
import { Logout } from '@components/auth/Logout';

export const Auth = () => {
  return (
    <div>
      <Login />
      <Logout />
    </div>
  );
};
